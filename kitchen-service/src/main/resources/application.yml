server:
  port: 8082

spring:
  application:
    name: kitchen-service

  datasource:
    url: jdbc:h2:mem:kitchendb;DB_CLOSE_DELAY=-1
    username: sa
    password: ''
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
